name: Container
on: push

jobs:
  node-docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: run Docker
        run: |
         ls
         cd tests
         ls

